fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.1
httpx>=0.27.0

# Optional, uncomment as the project takes shape:
# sqlalchemy>=2.0.0
# psycopg[binary]>=3.2.0
anthropic>=0.40.0
fastmcp>=2.12.0
# Optional for PromptCompressor LLMLingua-2 backend:
# llmlingua>=0.2.2
# --- Captain America engine ---
selectolax>=0.3.21          # fast HTML parsing for the httpx fallback collector
anthropic>=0.40.0           # Claude extractor + credibility capsule (optional, has_anthropic)
the-token-company>=0.3.2    # Transparent Anthropic/OpenAI prompt compression wrapper

# --- Optional integrations (import-guarded; absence degrades gracefully) ---
# Firecrawl is called through its HTTP API with httpx; no browser runtime is needed.
# Redis cache + rolling reputation:
# redis>=5.2.0
# Observability:
sentry-sdk>=2.18.0
# arize-phoenix-otel>=0.6.0
arize-otel>=0.6.0
openinference-instrumentation-anthropic>=1.0.6
# Generic (non-OpenInference) OTel instrumentors: these propagate W3C trace
# context across process boundaries (mcp_server.py -> FastAPI) and capture
# the inbound HTTP span, so an agent's MCP tool call and the engine's
# pipeline spans land in Arize as one connected trace.
opentelemetry-instrumentation-httpx>=0.48b0
opentelemetry-instrumentation-fastapi>=0.48b0
# MCP server (mcp_server.py) + MCP client (app/ml/terac_mcp_client.py speaks
# MCP to Terac's hosted endpoint, not fastmcp's server side):
mcp>=1.28.0
# Text credibility classifier trained from labeled Supabase tasks.
scikit-learn>=1.5.0
joblib>=1.4.0
